defmodule NectarNode do
  @moduledoc """
  Documentation for NectarNode.

  A distributed in-memory key-value store. Implements
  fast writes, read-repair, load-balancing, rollbacks,
  eventual consistency, distributed modegit 
  """

  def main(_) do
    IO.inspect "Hello"
  end
end
